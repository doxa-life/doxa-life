/**
 * svg-defaults.js — Defaults for the SVG class library (SvgCanvas etc.).
 * Used as fallback when individual shapes don't specify their own values.
 */

export const SVG_DEFAULTS = {
  stroke:       '#1f2937',
  strokeWidth:  1,
  fill:         '#3b82f6',
  fillOpacity:  0.6,
  fontFamily:   'sans-serif',
  fontSize:     12,
  textColor:    '#111827',
  background:   '#ffffff',
  viewBoxPad:   16,
};
