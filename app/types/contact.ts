// Message categories the contact form offers. The values are the feedback types
// the campaigns-server understands (`/api/contact` -> `feedback_type`); only the
// labels are worded for a contact-us page.
export type MessageType = 'compliment' | 'suggestion' | 'problem'
