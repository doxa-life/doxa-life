# Bundle outputs — regenerated on every build
app/
!app/.gitkeep

# Node
node_modules/
.npm/
.pnpm-store/

# Vite cache
.vite/
dist/

# Editor
.vscode/
.idea/

# OS
.DS_Store

# Env
.env
.env.local
.env.*.local
!.env.example

# Research dashboard — developed on its own branch (research-dashboard),
# excluded from the map-only bundle. Do not track dashboard files here.
app-profiles/doxa-research-dashboards/
src/components/dashboards/

# Local build artifacts / deploy config — kept on disk, out of the map PR
build-sizes-baseline.json
vendored-bundles.json
Dockerfile
.dockerignore
nginx.conf.template

# Sub-project lockfile — root bun.lock is authoritative
bun.lock
